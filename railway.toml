[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npx tsc --project tsconfig.server.json && npm run build"

[deploy]
startCommand = "npx prisma db push && node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
