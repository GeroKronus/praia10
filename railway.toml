[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma db push && npx ts-node --project tsconfig.server.json server.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
