[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx prisma generate && npx prisma db push && npm run build"

[deploy]
startCommand = "npx ts-node --project tsconfig.server.json server.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
